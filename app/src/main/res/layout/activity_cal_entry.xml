<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.gusain.arogya.CalEntryActivity">

    <TextView
        android:id="@+id/selectFood"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="57dp"
        android:ems="10"
        android:inputType="text"
        android:text="Select Food (100g/Piece)"
        android:textAlignment="center"
        android:textSize="24sp" />

    <Spinner
        android:id="@+id/spinner1"
        android:layout_width="350dp"
        android:layout_height="40dp"
        android:layout_marginTop="17dp"
        android:background="@drawable/fields_round"
        android:spinnerMode="dialog"
        android:layout_below="@+id/selectFood"
        android:layout_centerHorizontal="true" />

    <TextView
        android:id="@+id/quant"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/spinner1"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="62dp"
        android:ems="10"
        android:inputType="textPersonName"
        android:text="Quantity"
        android:textAlignment="center"
        android:textSize="24sp" />

    <Spinner
        android:id="@+id/spinner2"
        android:layout_width="350dp"
        android:layout_height="40dp"
        android:background="@drawable/fields_round"
        android:spinnerMode="dialog"
        android:layout_centerVertical="true"
        android:layout_alignStart="@+id/spinner1" />

    <Button
        android:id="@+id/foodentrySubmitBtn"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:background="@drawable/fields_round"
        android:text="Submit"
        android:layout_marginBottom="56dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
